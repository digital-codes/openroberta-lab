DIFFDRIVE:
  ASEBA: |
    sub diffdrive
        motor.left.target = MOTOR_MAX * speedL 
        motor.right.target =  MOTOR_MAX * speedR

STOP:
  ASEBA: |
    sub diffdrive_stop
        motor.left.target = 0
        motor.right.target = 0
